{
  "name": "office-orcs-survey",
  "version": "1.0.0",
  "private": true,
  "description": "Simple React quiz app served locally with npm",
  "scripts": {
    "start": "http-server . -p 3000 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
